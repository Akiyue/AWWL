torch>=2.1
torchvision
diffusers>=0.27
transformers>=4.40
accelerate>=0.28
datasets>=2.18
pytorch-wavelets>=1.3.0
PyWavelets>=1.4          # pytorch-wavelets imports pywt without declaring it
Pillow
numpy
pandas
tqdm
PyYAML>=6.0
typer>=0.12

# Evaluation extras (install with `pip install -e .[eval]`)
# clean-fid>=0.1.35
# torch-fidelity>=0.3.0
# prdc
# scipy

# Plotting extras (install with `pip install -e .[plot]`)
# matplotlib>=3.7
# seaborn>=0.13
