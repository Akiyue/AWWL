__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
.venv/
venv/
env/

# Run artifacts (regeneratable)
output/
runs/
generated_images/
results/
*.png
*.jpg
*.jpeg
*.csv

# Allow figures intentionally checked in
!assets/**/*.png

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Paper PDF — kept locally, distributed elsewhere
Cita_2025_Hanu.pdf
*.pdf

# Local-only scripts (history bootstrapper)
.history.py

# clean-fid caches its InceptionV3 weights into the CWD on first use
inception-2015-12-05.pt

# Paper drafts and analysis notes. Tracked now that the repository is
# private; make it public again only after submission, and check what is
# in here before you do.
AWWL_*.md

# LaTeX build products, not sources
paper/main.pdf
paper/tables/
paper/figures/
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
